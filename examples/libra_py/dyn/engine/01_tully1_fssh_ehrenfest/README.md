# Example 01: Tully-1 FSSH and Ehrenfest

Run `python compute.py`, followed by `python plot.py`. All generated files are
kept in this directory's `output/` and `plots/` subdirectories. See the parent
README for the numerical options and generated figures.
