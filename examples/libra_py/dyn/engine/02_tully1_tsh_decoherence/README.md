# Example 02: Tully-1 hopping and decoherence

Run `python compute.py`, followed by `python plot.py`. The computation covers
FSSH, GFSH, and FSSH2 with coherent, ID-A, and SDM electronic dynamics. See the
parent README for details and quick-run command-line options.
